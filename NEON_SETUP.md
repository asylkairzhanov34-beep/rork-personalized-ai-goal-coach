# Настройка Neon Database для аутентификации

## Шаг 1: Создание базы данных на Neon

1. Перейдите на [neon.tech](https://neon.tech)
2. Зарегистрируйтесь (можно через GitHub)
3. Нажмите **"Create a project"**
4. Выберите регион (ближайший к вам или вашим пользователям)
5. Нажмите **"Create project"**

## Шаг 2: Получение Connection String

1. После создания проекта, вы увидите Dashboard
2. Найдите раздел **"Connection String"**
3. Выберите **"Pooled connection"** (рекомендуется для serverless)
4. Скопируйте строку подключения. Она будет выглядеть так:
   ```
   postgresql://user:password@ep-xxx-xxx.region.neon.tech/dbname?sslmode=require
   ```

## Шаг 3: Добавление в env файл

1. Откройте файл `env` в корне проекта
2. Найдите строку `DATABASE_URL=`
3. Вставьте скопированную Connection String:
   ```env
   DATABASE_URL=postgresql://user:password@ep-xxx-xxx.region.neon.tech/dbname?sslmode=require
   ```

## Шаг 4: Создание таблиц

1. В Neon Dashboard перейдите в **SQL Editor** (слева в меню)
2. Скопируйте и выполните следующий SQL:

```sql
-- Создание таблицы пользователей
CREATE TABLE IF NOT EXISTS users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email TEXT UNIQUE,
  name TEXT,
  apple_id TEXT UNIQUE,
  password_hash TEXT,
  is_premium BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Индексы для быстрого поиска
CREATE INDEX IF NOT EXISTS idx_users_email ON users(email);
CREATE INDEX IF NOT EXISTS idx_users_apple_id ON users(apple_id);

-- Проверка, что таблица создана
SELECT * FROM users LIMIT 1;
```

3. Нажмите **"Run"** или **Ctrl+Enter**
4. Вы должны увидеть сообщение об успешном выполнении

## Шаг 5: Проверка подключения

1. Перезапустите сервер разработки
2. В консоли вы должны увидеть:
   ```
   [DB] Initializing database connection...
   [DB] Database connection initialized successfully
   ```

## Готово! 🎉

Теперь ваше приложение подключено к Neon:
- Apple Sign In будет автоматически создавать пользователей в базе
- Сессии хранятся локально на устройстве
- Данные пользователей безопасно хранятся в Neon

## Что дальше?

- Проверьте вход через Apple Sign In на iOS устройстве
- В Neon Dashboard -> Tables вы увидите созданных пользователей
- Можете добавить дополнительные поля в таблицу users при необходимости

## Преимущества Neon

✅ Бесплатный план с 0.5 ГБ хранилища  
✅ Автоматическое масштабирование  
✅ Serverless - платите только за использование  
✅ Встроенный SQL Editor  
✅ Автоматические бэкапы  
✅ Поддержка PostgreSQL 15+

## Полезные ссылки

- [Neon Documentation](https://neon.tech/docs)
- [Drizzle ORM Documentation](https://orm.drizzle.team/docs/overview)
- [PostgreSQL Tutorial](https://www.postgresql.org/docs/current/tutorial.html)
