# TestFlight Offerings Debug Guide

## Проблема
В TestFlight не загружаются offerings (пакеты подписок) из RevenueCat, кнопка "Попробовать Premium" неактивна, и цены не отображаются.

## Что было сделано

### 1. Добавлено подробное логирование
Теперь в консоли вы увидите детальную информацию о каждом шаге:

**В app/_layout.tsx:**
- ✅ Инициализация RevenueCat
- ✅ Загрузка offerings
- ✅ Информация о каждом пакете

**В lib/revenuecat.ts:**
- ✅ Статус модуля (загружен/не загружен)
- ✅ Статус конфигурации
- ✅ Raw response от RevenueCat API
- ✅ Детали каждого пакета (identifier, title, price)

**В hooks/use-subscription-store.tsx:**
- ✅ Процесс загрузки offerings
- ✅ Результат (количество пакетов, ошибки)

### 2. Добавлен индикатор загрузки
Теперь на экране подписки вместо пустого места показывается:
- Loader (ActivityIndicator)
- Текст "Загрузка планов подписки..." или "Инициализация..."
- Кнопка "Открыть Debug Tools" (только в TestFlight/dev)

### 3. Hardcoded API Key
API ключ `appl_NIzzmGwASbGFsnfAddnshynSnsG` теперь хардкоден в коде, чтобы он точно работал на реальных устройствах.

## Как проверить в TestFlight

### Шаг 1: Посмотрите логи при запуске
Откройте консоль и найдите эти логи:

```
==================== REVENUECAT INIT ====================
📱 [_layout.tsx] Платформа: ios
📱 [_layout.tsx] Реальное устройство: true
📱 [_layout.tsx] API Key присутствует: ДА (appl_NIzzm...)
📱 [_layout.tsx] MOCK MODE: false
========================================================
```

### Шаг 2: Проверьте конфигурацию

Должны быть логи:
```
✅ [_layout.tsx] RevenueCat успешно инициализирован!
✅ [_layout.tsx] Debug логирование включено
```

### Шаг 3: Проверьте загрузку offerings

Ищите эти логи:
```
📦 [_layout.tsx] Загрузка offerings...
📦 [_layout.tsx] Offerings загружены: <название offering>
📦 [_layout.tsx] Доступные пакеты (X):
  1. <identifier> - <price>
  2. ...
```

### Шаг 4: Если offerings НЕ загрузились

**Возможные причины:**

#### A. Offerings не настроены в RevenueCat Dashboard
1. Зайдите в RevenueCat Dashboard
2. Перейдите в **Products** → проверьте, что продукты добавлены
3. Перейдите в **Offerings** → убедитесь, что есть **Current offering**
4. Проверьте, что в текущем offering есть доступные пакеты

#### B. Product IDs не совпадают
1. Проверьте Product IDs в App Store Connect
2. Убедитесь, что они совпадают с Product IDs в RevenueCat
3. Формат обычно: `com.yourapp.premium_monthly` или `premium_monthly`

#### C. Sandbox тестер не залогинен
1. На устройстве: **Settings → App Store → Sign Out**
2. Запустите приложение и попробуйте купить
3. При покупке войдите с Sandbox тестером
4. Проверьте, что видите **[Sandbox Environment]** в диалоге покупки

#### D. Статус подписки в RevenueCat
1. RevenueCat Dashboard → **Customers**
2. Найдите ваш тестовый App User ID
3. Проверьте статус entitlements

### Шаг 5: Проверьте логи RevenueCat SDK

В консоли должны быть **NATIVE** логи от RevenueCat SDK:
```
[Purchases] - DEBUG ℹ️ Vending products from cache
[Purchases] - DEBUG ℹ️ Products request completed: <products>
```

Если их нет, значит:
- RevenueCat SDK не загрузился
- Или products/offerings не настроены в dashboard

## Типичные ошибки и решения

### Ошибка: "NO availablePackages"
**Причина:** В RevenueCat Dashboard нет Current offering или он пустой  
**Решение:**
1. RevenueCat Dashboard → Offerings
2. Создайте offering или сделайте один из них Current
3. Добавьте пакеты в offering

### Ошибка: "Module not ready"
**Причина:** RevenueCat SDK не инициализировался  
**Решение:**
1. Проверьте, что react-native-purchases установлен
2. Проверьте, что это не Expo Go (нужен development build)
3. Пересоберите приложение: `npx expo prebuild --clean` → `eas build`

### Ошибка: "Invalid Product IDs"
**Причина:** Product IDs не совпадают между App Store Connect и RevenueCat  
**Решение:**
1. App Store Connect → My Apps → [Your App] → In-App Purchases
2. Скопируйте Product IDs
3. RevenueCat Dashboard → Products → убедитесь, что IDs совпадают

### Кнопка неактивна, но offerings загрузились
**Причина:** `packages.length === 0` в компоненте  
**Решение:** Проверьте логи `[SubscriptionProvider]`:
```
[SubscriptionProvider] ✅ Loaded offerings: X packages
[SubscriptionProvider] Package 1: <id> - <price>
```

Если пакеты есть в логах, но не в UI:
- Проблема в маппинге данных
- Проверьте `loadOfferingsFromRevenueCat` в `use-subscription-store.tsx`

## Debug Tools Screen

Откройте `/dev-subscription-tools` для:
- Просмотра текущего статуса подписки
- Принудительного обновления с сервера RevenueCat
- Сброса подписки для тестирования
- Просмотра offerings и packages

## Чек-лист перед тестом

- [ ] RevenueCat API Key добавлен в проект (hardcoded в lib/revenuecat.ts)
- [ ] Products созданы в App Store Connect
- [ ] Products добавлены в RevenueCat Dashboard
- [ ] Offering настроен и помечен как Current
- [ ] Пакеты добавлены в Current offering
- [ ] Sandbox тестер создан в App Store Connect
- [ ] TestFlight build установлен на реальном устройстве
- [ ] Вышли из App Store аккаунта перед покупкой

## Контакты для помощи

Если offerings всё ещё не загружаются:
1. Отправьте логи из консоли (все строки с [RevenueCat] и [SubscriptionProvider])
2. Скриншот RevenueCat Dashboard → Offerings
3. Скриншот App Store Connect → In-App Purchases
