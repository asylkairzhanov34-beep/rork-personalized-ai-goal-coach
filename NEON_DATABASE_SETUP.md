# 📊 Настройка базы данных Neon — Полная инструкция

Эта инструкция проведёт вас через все этапы настройки базы данных Neon для вашего приложения.

---

## 📋 Краткий чек-лист

- [ ] Шаг 1: Создать аккаунт на Neon
- [ ] Шаг 2: Создать проект
- [ ] Шаг 3: Скопировать Connection String
- [ ] Шаг 4: Создать таблицу через SQL Editor
- [ ] Шаг 5: Прислать мне DATABASE_URL

---

## Шаг 1: Создание аккаунта Neon

1. Откройте браузер и перейдите на:
   ```
   https://neon.tech
   ```

2. Нажмите кнопку **"Sign Up"** (в правом верхнем углу)

3. Выберите способ регистрации:
   - **Рекомендуется**: Вход через GitHub (быстрее всего)
   - Или через Google
   - Или через Email

4. Подтвердите email, если потребуется

---

## Шаг 2: Создание проекта

После входа в аккаунт:

1. Нажмите кнопку **"Create Project"** (или "New Project")

2. Заполните поля:

   | Поле | Значение |
   |-------|----------|
   | **Project name** | `goal-coach-app` |
   | **Database name** | `goal_coach_db` |
   | **Region** | `AWS eu-central-1` (Европа) или `AWS us-east-1` (США) |
   | **Postgres version** | Оставьте по умолчанию (16 или 17) |

3. Нажмите **"Create Project"**

4. Дождитесь создания (10-30 секунд)

---

## Шаг 3: Получение Connection String

После создания проекта вы окажетесь на странице Dashboard:

1. Найдите блок **"Connection Details"** (обычно справа)

2. Убедитесь, что выбран **"Connection string"** (не "Parameters")

3. Нажмите иконку глаза 👁️ **"Show password"**

4. Нажмите иконку копирования 📋 (или выделите всю строку и скопируйте)

Строка выглядит примерно так:
```
postgresql://goal_coach_db_owner:abc123xyz@ep-cool-paper-123456.eu-central-1.aws.neon.tech/goal_coach_db?sslmode=require
```

**❗ ВАЖНО**: Сохраните эту строку — она понадобится в Шаге 5!

---

## Шаг 4: Создание таблицы users

1. В левом меню нажмите **"SQL Editor"**

2. Скопируйте и вставьте этот SQL-код:

```sql
-- Создание таблицы пользователей
CREATE TABLE IF NOT EXISTS users (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  email TEXT UNIQUE,
  name TEXT,
  apple_id TEXT UNIQUE,
  password_hash TEXT,
  is_premium BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Индекс для быстрого поиска по Apple ID
CREATE INDEX IF NOT EXISTS idx_users_apple_id ON users(apple_id);

-- Индекс для быстрого поиска по email
CREATE INDEX IF NOT EXISTS idx_users_email ON users(email);
```

3. Нажмите кнопку **"Run"** (или Ctrl+Enter / Cmd+Enter)

4. Убедитесь, что выполнение успешно (зелёная галочка или сообщение "Query completed")

---

## Шаг 5: Пришлите мне DATABASE_URL

После выполнения шагов 1-4, пришлите мне строку подключения в таком формате:

```
DATABASE_URL=postgresql://...ваша_строка...
```

**Пример:**
```
DATABASE_URL=postgresql://goal_coach_db_owner:abc123xyz@ep-cool-paper-123456.eu-central-1.aws.neon.tech/goal_coach_db?sslmode=require
```

Я добавлю её в настройки проекта и проверю подключение.

---

## 🛡️ Безопасность

- ✅ Никогда не публикуйте DATABASE_URL в публичных репозиториях
- ✅ Neon автоматически шифрует соединения (sslmode=require)
- ✅ Бесплатный тариф Neon включает 0.5 GB хранилища (достаточно для старта)

---

## ❓ Частые вопросы

**Q: Это бесплатно?**
A: Да, Neon имеет бесплатный тариф (Free Tier) с 0.5 GB хранилища и 191 часов вычислений в месяц.

**Q: Какой регион выбрать?**
A: Выбирайте ближайший к вашим пользователям. Для России/СНГ подойдёт `eu-central-1` (Франкфурт).

**Q: Что если я забыл пароль?**
A: Можно сбросить в Dashboard → Settings → Reset password.

---

## 📞 После настройки

Когда я получу ваш DATABASE_URL, я:
1. Добавлю его в настройки проекта
2. Проверю подключение к базе
3. Убедюсь, что всё работает

---

## 🔗 Полезные ссылки

- Политика конфиденциальности приложения: https://www.notion.so/PRIVACY-POLICY-AND-COOKIES-2b44e106d5d0807aaff8e5765d4b8539
- Neon Dashboard: https://console.neon.tech
