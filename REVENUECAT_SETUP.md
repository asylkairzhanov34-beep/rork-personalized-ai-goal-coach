# Настройка RevenueCat для iOS (с Windows)

Это руководство поможет вам настроить покупки через RevenueCat для iOS, используя только Windows и веб-интерфейсы.

## Шаг 1: Создание аккаунта Apple Developer

1. Перейдите на https://developer.apple.com
2. Зарегистрируйтесь как разработчик (платная подписка $99/год)
3. После оплаты, аккаунт будет активирован в течение 24-48 часов

## Шаг 2: Настройка приложения в App Store Connect

1. Перейдите на https://appstoreconnect.apple.com
2. Войдите с вашим Apple ID
3. Нажмите "My Apps" → "+" → "New App"
4. Заполните информацию:
   - **Platform**: iOS
   - **Name**: GoalForge
   - **Primary Language**: Russian
   - **Bundle ID**: Создайте новый (например: `app.rork.goalforge`)
   - **SKU**: Уникальный идентификатор (например: `goalforge-001`)
   - **User Access**: Full Access

## Шаг 3: Создание продуктов подписки

1. В App Store Connect откройте ваше приложение
2. Перейдите в раздел "Subscriptions" (слева в меню)
3. Нажмите "+" → "Create Subscription Group"
4. Назовите группу (например: "Premium Subscriptions")
5. Создайте продукты подписки:

### Месячная подписка
- **Reference Name**: Premium Monthly
- **Product ID**: `premium_monthly` (запомните это ID!)
- **Subscription Duration**: 1 Month
- **Price**: Выберите цену (например, $9.99)

### Годовая подписка (опционально)
- **Reference Name**: Premium Yearly
- **Product ID**: `premium_yearly`
- **Subscription Duration**: 1 Year
- **Price**: Выберите цену (например, $79.99)

**ВАЖНО**: Сохраните Product ID - они понадобятся для настройки в RevenueCat!

## Шаг 4: Создание аккаунта в RevenueCat

1. Перейдите на https://www.revenuecat.com
2. Нажмите "Sign Up" и создайте бесплатный аккаунт
3. Подтвердите email

## Шаг 5: Создание проекта в RevenueCat

1. После входа нажмите "Create new project"
2. Введите название проекта (например: "GoalForge")
3. Выберите платформу: **iOS**

## Шаг 6: Настройка iOS приложения

1. В RevenueCat Dashboard перейдите в "Projects" → ваш проект
2. Нажмите "Add app" → "iOS"
3. Заполните информацию:
   - **App name**: GoalForge
   - **Bundle ID**: `app.rork.goalforge` (тот же, что в App Store Connect)

## Шаг 7: Подключение App Store Connect

1. В настройках iOS приложения в RevenueCat найдите раздел "App Store Connect"
2. Вам нужно создать **In-App Purchase Key**:
   
   a. Откройте https://appstoreconnect.apple.com
   b. Перейдите в "Users and Access" → "Keys" → "In-App Purchase"
   c. Нажмите "+" для создания нового ключа
   d. Назовите ключ (например: "RevenueCat Key")
   e. Скачайте .p8 файл (ВАЖНО: сохраните его, он будет показан только один раз!)
   f. Сохраните **Key ID** и **Issuer ID**

3. В RevenueCat вернитесь к настройкам приложения:
   - Загрузите .p8 файл
   - Введите **Key ID**
   - Введите **Issuer ID**
   - Нажмите "Save"

## Шаг 8: Создание продуктов в RevenueCat

1. В RevenueCat Dashboard перейдите в "Products"
2. Нажмите "Add Product"
3. Для каждого продукта из App Store Connect:
   - **Product Identifier**: Точно такой же, как Product ID в App Store (`premium_monthly`)
   - **Type**: Subscription
   - **Store**: App Store
4. Нажмите "Save"

## Шаг 9: Создание Entitlements

1. Перейдите в раздел "Entitlements"
2. Нажмите "Add Entitlement"
3. Создайте:
   - **Identifier**: `premium` (это ID будет использоваться в коде)
   - **Products**: Выберите все созданные продукты (`premium_monthly`, `premium_yearly`)

## Шаг 10: Получение API ключей RevenueCat

1. В RevenueCat Dashboard перейдите в проект
2. Нажмите на название проекта → "API Keys"
3. Найдите секцию "App specific keys"
4. Скопируйте **Public iOS SDK Key** (начинается с `appl_...`)

## Шаг 11: Добавление ключей в код

Откройте файл `hooks/use-subscription-store.tsx` и замените:

```typescript
const REVENUECAT_API_KEY = {
  ios: 'YOUR_IOS_API_KEY_HERE',  // Вставьте ваш Public iOS SDK Key
  android: 'YOUR_ANDROID_API_KEY_HERE',
};
```

На:

```typescript
const REVENUECAT_API_KEY = {
  ios: 'appl_ваш_ключ_здесь',
  android: 'YOUR_ANDROID_API_KEY_HERE',
};
```

## Шаг 12: Тестирование

### Подготовка к тестированию

1. В App Store Connect перейдите в "Users and Access" → "Sandbox Testers"
2. Нажмите "+" и создайте тестового пользователя:
   - Email: любой несуществующий email
   - Password: придумайте пароль
   - Country: Russia

### Тестирование на устройстве

1. Откройте приложение на физическом iOS устройстве (НЕ симулятор!)
2. Перейдите на экран подписки
3. При попытке покупки:
   - iOS попросит войти в App Store
   - Войдите с данными **Sandbox Tester** (не с вашим основным Apple ID!)
   - Совершите покупку - деньги НЕ будут списаны (это тестовая среда)

**ВАЖНО**: 
- Симулятор не поддерживает покупки - только физическое устройство!
- Используйте только Sandbox тестеров, не ваш основной Apple ID
- В тестовой среде деньги не списываются

## Шаг 13: Интеграция в UI

Добавьте кнопку для открытия экрана подписки в любом месте приложения:

```typescript
import { useRouter } from 'expo-router';
import { useSubscription } from '@/hooks/use-subscription-store';

function YourComponent() {
  const router = useRouter();
  const { isPremium } = useSubscription();

  return (
    <TouchableOpacity onPress={() => router.push('/subscription')}>
      <Text>{isPremium ? 'Premium активна' : 'Получить Premium'}</Text>
    </TouchableOpacity>
  );
}
```

## Шаг 14: Публикация (когда готовы)

1. В App Store Connect заполните все обязательные поля:
   - Скриншоты
   - Описание
   - Keywords
   - Privacy Policy URL
   - Support URL

2. В разделе "Pricing and Availability":
   - Выберите страны
   - Цены подтянутся автоматически из настроек продуктов

3. Отправьте приложение на ревью через EAS Build
   (но для этого вам понадобится помощь кого-то с Mac или использование облачного билда)

## Проверка статуса подписки в коде

Используйте хук `useSubscription` для проверки статуса:

```typescript
import { useSubscription } from '@/hooks/use-subscription-store';

function FeatureComponent() {
  const { isPremium, status } = useSubscription();

  if (!isPremium) {
    return (
      <View>
        <Text>Эта функция доступна только в Premium</Text>
        <Button title="Получить Premium" onPress={() => router.push('/subscription')} />
      </View>
    );
  }

  return <PremiumFeature />;
}
```

## Полезные ссылки

- RevenueCat Dashboard: https://app.revenuecat.com
- App Store Connect: https://appstoreconnect.apple.com
- RevenueCat Docs: https://docs.revenuecat.com
- Expo + RevenueCat: https://docs.revenuecat.com/docs/expo

## Поддержка

Если возникнут проблемы:
1. Проверьте Bundle ID - он должен совпадать везде
2. Убедитесь, что Product ID точно совпадают
3. Проверьте, что используете Sandbox тестера
4. Посмотрите логи в консоли приложения
5. Проверьте RevenueCat Dashboard → Customer → найдите вашего тестера

## Важные замечания

⚠️ **Без Mac вы НЕ сможете**:
- Собрать binary (.ipa файл) для тестирования
- Загрузить приложение в App Store напрямую

✅ **Что вы МОЖЕТЕ сделать с Windows**:
- Настроить все в веб-интерфейсах
- Написать весь код
- Использовать Expo Go для тестирования (но покупки не будут работать в Expo Go!)
- Подготовить приложение к публикации

💡 **Решение**: 
- Используйте EAS Build (облачный сервис Expo) для создания билдов
- Или попросите кого-то с Mac помочь с финальной сборкой
